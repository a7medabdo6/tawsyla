/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // No i18n configuration here - we'll handle locale switching client-side only
};

module.exports = nextConfig;
